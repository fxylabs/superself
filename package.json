{
  "name": "superself-monorepo",
  "description": "Agent workspace for shipping AI-built products",
  "license": "Apache-2.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "structure": "pnpm --filter superself structure",
    "typecheck": "pnpm -r typecheck"
  }
}
