{
  "name": "superself-monorepo",
  "description": "Agent workspace for shipping AI-built products",
  "license": "Apache-2.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "pnpm --filter superself build",
    "typecheck": "pnpm -r typecheck",
    "proof": "pnpm -r proof"
  }
}
