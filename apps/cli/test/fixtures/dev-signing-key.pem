# ⚠ DEVELOPMENT SIGNING KEY — PUBLIC BY CONSTRUCTION ⚠
#
# This is the private half of the `dev-2026a` keypair whose public half is
# pinned in `src/releasekeys.ts`. It lives in a public repository, so anything
# it signs is signed by a key everyone on earth already has.
#
# It exists so the plugin suites can build real signed release documents and
# drive the loader end to end. It MUST NEVER sign a production release, and the
# release checklist replaces the pinned `dev-2026a` record before the first
# public release ships.
#
# PEM parsers ignore everything before the BEGIN line, so this header costs
# nothing at read time.
-----BEGIN PRIVATE KEY-----
MC4CAQAwBQYDK2VwBCIEIEgSzvnk449Vo881x/l/37JI3CPZ9bVTllC8NACoABGF
-----END PRIVATE KEY-----
