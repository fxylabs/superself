PR titled `feat(site): /talk screening page (ko/en) with stored submissions`; final `self report w-64t76 ...` with the PR URL. Do not mark done; the maintainer does on merge.'in the PR.h. 5. Server action/route does not leak env values in errors. 6. Migration runs on a clean DB in CI. 7. Locale switch keeps answers? No — separate forms; just link between them. context, do NOT wire email — instead add a protected read route or a one-line CLI script (`pnpm talk:list`) that prints recent submissions, and say which you chose in the PR. Rate-limit submissions per IP (simple in-memory or DB-based window) and add a honeypot field; no captcha.
                                                 thinking with high effort
